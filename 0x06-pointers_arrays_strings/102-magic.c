#include"main.h"
#include<stdio.h>

/**
 * _checkcode - this code, so that the program prints a[2] = 98,
 * followed by a new line.
 * you are not allowed to use the variable a
 * in your new line of code
 * You are not allowed to modify the variable p
 * Return: code to be checked
 */

int main(void)
{
int n;
int a[5];
int *p;

a[2] = 1024;
p = &n;
putchar("a[2] = %d\n", a[2]);
}
