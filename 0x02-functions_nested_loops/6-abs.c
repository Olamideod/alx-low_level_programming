#include "main.h"
/**
* Main: Start point
* Description - returns absolute value of an int
* from which to get an absolute value
* Return: Void.
*/

int _abs(int n)
{

if (n < 0)
{
return (n * -1);
}
return (n);
}

